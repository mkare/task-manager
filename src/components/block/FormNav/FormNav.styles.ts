import styled from "styled-components";
import { spacer } from "@styled/variables";

export const FormNavStyled = styled.nav`
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin: ${spacer.md} 0;
`;
